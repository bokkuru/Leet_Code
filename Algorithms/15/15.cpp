class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<int> two_num;
        vector<vector<int>> twosum;
        for(int i = 0; i < nums.size(); i++){
            for(int j = 0; j < two_num; j++){
                if(two_num[j] + nums[i] == 0){
                    
                }
            }
        }
    }
};